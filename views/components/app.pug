script#app-template(type='text/x-template')
  .container.mb-5
    h1.mt-3.text-center
      | Locator

    form(@submit='submit')
      .form-row
        .col-12.col-md-8.col-lg-6.col-xl-4.offset-md-2.offset-lg-3.offset-xl-4
          input.form-control(placeholder='IP address', v-model='ip')
      .text-center
        button.mt-2(type='submit', class='btn btn-primary') Locate

    p.lead.text-center.mt-3(v-if='hasIpLocation()')
      | You are in {{ getHumanLocation() }}
